= link_to group_chats_path(group) do
  %group_list
    %ul
      %li.group-name
        = group.name
      %li.current-chat
        - if group.chats.last
          = group.chats.last.try!(:body)
        - else
          .nil_chat まだメッセージの投稿はありません
